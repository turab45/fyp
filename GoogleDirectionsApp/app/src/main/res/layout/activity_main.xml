<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/driver_btn"
        android:layout_width="202dp"
        android:layout_height="68dp"
        android:onClick="onClick"
        android:text="Driver"
        app:layout_constraintBottom_toTopOf="@+id/student_btn"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/admin_btn"
        tools:ignore="MissingConstraints" />

    <Button
        android:id="@+id/student_btn"
        android:layout_width="202dp"
        android:layout_height="68dp"
        android:onClick="onClick"
        android:text="Student"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/driver_btn"
        tools:ignore="MissingConstraints" />

    <Button
        android:id="@+id/admin_btn"
        android:layout_width="202dp"
        android:layout_height="68dp"
        android:onClick="onClick"
        android:text="Admin"
        app:layout_constraintBottom_toTopOf="@+id/driver_btn"
        app:layout_constraintEnd_toEndOf="@+id/driver_btn"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="@+id/driver_btn"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="spread"
        tools:ignore="MissingConstraints" />
</androidx.constraintlayout.widget.ConstraintLayout>